0. pipx run cookiecutter

1.
git rm env_sample
git rm -rf ocr
git rm -rf tests
#

poetry add django  dj-database-url psycopg gunicorn
poetry add --group dev pytest-django

django-admin startproject ocr .

update manage.py to run load_dotenv()

update settings.py
   TIME_ZONE value
   disabled admin
   logging config

updated Dockerfile and entrypoint.sh for building container image

2.

python manage.py startapp statements

create Statement model
add settings_test.py and configure it in pyproject.toml
updated conftest.py to populate database with seed data
added unit tests for checking seed data


3.

poetry add django-tables2 django-bootstrap5

add statement_list for listing Statements in a table
add statement_upload page for uploading new statement
add test_views.py

4.
poetry add google-cloud-documentai django-q2

add cdoe logic to submit calling DocumentAI API with async queue
add Procfile
